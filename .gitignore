node_modules/
# 也忽略 node_modules 软链接本身：worktree 复用源项目依赖时创建的是符号链接文件，上面带斜杠的目录规则匹配不到。
node_modules

# 构建与发布产物
dist/
dist-electron/
dist-ssr/
release/
coverage/
.nyc_output/
*.dmg
*.exe
*.zip
*.tar.gz
*.blockmap
latest*.yml
builder-debug.yml
builder-effective-config.yaml

# 本地环境与日志
.DS_Store
# 项目提交公开、无凭据的 npmmirror 配置；个人私有 registry/token 必须放用户级 ~/.npmrc。
.env
.env.*
!.env.example
*.local
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# 包管理器：本项目使用 pnpm，避免混入 npm/yarn 锁文件
package-lock.json
yarn.lock

# 本地 AI/工具状态，不应进入仓库
.claude/*.local.md
.superpowers/
.agents/
docs/superpowers/

# 旧桌面框架或工具链缓存
src-tauri/target/

# 编辑器本地状态
.idea/
.vscode/
